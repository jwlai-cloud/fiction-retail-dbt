# Ingested via datahub/glossary_ingest.yml (datahub-business-glossary source).
# The agent compares the version of these terms live in DataHub against
# what a PR proposes — see ADR-0005.
version: "1"
source: DataHub
owners:
  users:
    - datahub
nodes:
  - name: Commerce Metrics
    description: Revenue and customer metrics for the fiction-retail demo.
    terms:
      - name: Gross Revenue
        description: >
          Sum of order_amount_usd over fulfilled (completed or shipped)
          orders in the period, refunds excluded.
          revenue, never here.
        term_source: INTERNAL
      - name: Active Customer
        description: >
          A customer with at least one non-cancelled order in the trailing
          90 days.
        term_source: INTERNAL
